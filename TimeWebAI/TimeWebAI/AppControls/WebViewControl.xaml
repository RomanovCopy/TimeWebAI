<UserControl x:Class="TimeWebAI.AppControls.WebViewControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:i="http://schemas.microsoft.com/xaml/behaviors"
             xmlns:markup="clr-namespace:TimeWebAI.Markup"
             xmlns:interfaces="clr-namespace:TimeWebAI.Interfaces"
             xmlns:local="clr-namespace:TimeWebAI.AppControls" 
             mc:Ignorable="d"
             d:DataContext="{x:Type interfaces:IWebViewControlViewModel}">
    <UserControl.DataContext>
        <markup:ResolveViewModel ViewModelType="{x:Type interfaces:IWebViewControlViewModel}"/>
    </UserControl.DataContext>
    <Grid>
        <Border  BorderBrush="Black" BorderThickness="1,1,1,0" >
            <ContentControl Content="{markup:ResolveUserControl UserControlType={x:Type interfaces:IWebViewService}}"/>
        </Border>
        <i:Interaction.Triggers>
            <i:EventTrigger EventName="Loaded">
                <i:InvokeCommandAction Command="{Binding Loaded, UpdateSourceTrigger=PropertyChanged}"/>
            </i:EventTrigger>
        </i:Interaction.Triggers>
    </Grid>
</UserControl>
